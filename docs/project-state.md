# Dogfood project state

Version: 0.1.0 development build.

The user approved autonomous improvements during tasks and while away, broader improvements, persistent findings, installation by other users, and self-improvement.

The Python engine and local workflow are tested. The GitHub project state adapter passed an interactive read, guarded claim, source change, and checkpoint round trip. Its evidence is in runtime/evidence/github-adapter.json on the dogfood-state branch. The changed instructions await fresh-run activation evidence.

This repository is the selected source destination. A native daily Improve Dogfood task is enabled for this repository, with a three-change or 20-minute budget. No completed unattended run has been observed. No marketplace registration was created. User findings remain outside the shared source package on dogfood-state; that public state may contain only this project's public code and documentation findings.

Root AGENTS.md now supplies project startup instructions. Codex fresh-session activation, Work-wide activation, a completed unattended run, self-update behavior in a fresh model run, and installation by a second user still need verification. See work-activation.md for the remaining Work instruction step.
